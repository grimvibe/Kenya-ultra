services:
  - type: web
    name: kenya-ultra
    runtime: node
    plan: free

    buildCommand: npm install

    startCommand: npm start

    autoDeploy: true

    healthCheckPath: /

    envVars:
      - key: SESSION_ID
        sync: false
